PORT=3006

# MongoDB (local)
MONGODB_URI=mongodb://127.0.0.1:27017/courses

# Auth (no DB — checked against these directly)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me

# Session
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=7d
